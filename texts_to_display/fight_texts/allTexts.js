const missedAttackedTexts = [
    
];

function capitalizeFirstLetter(stringToCapitalize) {
    return stringToCapitalize[0].toUpperCase() + stringToCapitalize.slice(1);
}

export { capitalizeFirstLetter };